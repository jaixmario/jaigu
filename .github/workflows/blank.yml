name: Cracker-RDP

on: [push, workflow_dispatch]

jobs:
  build:
    runs-on: windows-2022

    steps:
    - name: Enable RDP
      run: |
        Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server' -Name "fDenyTSConnections" -Value 0
        Enable-NetFirewallRule -DisplayGroup "Remote Desktop"
        Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server\WinStations\RDP-Tcp' -Name "UserAuthentication" -Value 1

    - name: Set RDP Password
      run: Set-LocalUser -Name "runneradmin" -Password (ConvertTo-SecureString -AsPlainText "P@ssw0rd!" -Force)

    - name: Enable OpenSSH Client
      run: |
        Get-WindowsCapability -Online | Where-Object Name -like 'OpenSSH.Client*' | ForEach-Object {
          if ($_.State -ne 'Installed') {
            Add-WindowsCapability -Online -Name $_.Name
          }
        }

    - name: Start Serveo Tunnel
      run: ssh -o ServerAliveInterval=60 -o ExitOnForwardFailure=yes -N -T -R 3389:localhost:3389 serveo.net
